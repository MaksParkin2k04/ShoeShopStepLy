# Микросервисная архитектура ShoeShop

## Структура проектов

### 1. ShoeShop.API (Основной API)
- **Порт**: 7001
- **Назначение**: REST API для всех клиентов
- **Swagger**: https://localhost:7001/swagger
- **Эндпоинты**:
  - `GET /api/products` - получить товары
  - `GET /api/products/{id}` - получить товар по ID
  - `POST /api/products` - создать товар
  - `GET /api/orders` - получить заказы
  - `POST /api/orders` - создать заказ
  - `PUT /api/orders/{id}/status` - обновить статус заказа

### 2. ShoeShop (Web сайт)
- **Порт**: 7002
- **Назначение**: Веб-сайт (клиент API)
- **Обращается к**: ShoeShop.API

### 3. ShoeShop.TelegramBot
- **Назначение**: Telegram бот
- **Обращается к**: ShoeShop.API
- **Токен**: 8468206640:AAFKsz7TklbKeaQbTIsmu__DzU01KK2sx1U

### 4. ShoeShop.VKBot (планируется)
- **Назначение**: VK бот
- **Обращается к**: ShoeShop.API

### 5. ShoeShop.Shared
- **Назначение**: Общие DTO и модели
- **Используется**: Всеми проектами

## Запуск проектов

### API (обязательно первым)
```bash
cd ShoeShop.API
dotnet run --urls "https://localhost:7001"
```

### Telegram Bot
```bash
cd ShoeShop.TelegramBot
dotnet run
```

### Web сайт
```bash
cd ShoeShop
dotnet run --urls "https://localhost:7002"
```

## Обмен данными

Все клиенты (сайт, Telegram, VK) обращаются только к API:
- **Сайт** → API
- **Telegram Bot** → API  
- **VK Bot** → API

API работает с базой данных и возвращает данные в формате JSON.

## Преимущества архитектуры

1. **Разделение ответственности** - каждый проект решает свою задачу
2. **Масштабируемость** - можно запускать несколько экземпляров каждого сервиса
3. **Независимость** - изменения в одном проекте не влияют на другие
4. **Единый API** - все данные проходят через один источник
5. **Легкое тестирование** - API можно тестировать через Swagger

## Запуск системы

### Быстрый запуск
```bash
# Запуск всех сервисов
START_SERVICES.bat

# Остановка всех сервисов
STOP_SERVICES.bat
```

### Ручной запуск
```bash
# 1. API (обязательно первым)
cd ShoeShop.API
dotnet run --urls "https://localhost:7001"

# 2. Telegram Bot
cd ShoeShop.TelegramBot
dotnet run

# 3. Web сайт
cd ShoeShop
dotnet run --urls "https://localhost:7002"

# 4. VK Bot (опционально)
cd ShoeShop.VKBot
dotnet run
```

### Docker запуск
```bash
docker-compose up -d
```

## Доступ к сервисам

- **API Swagger**: https://localhost:7001/swagger
- **Web сайт**: https://localhost:7002
- **Telegram Bot**: @your_bot_name

## Следующие шаги

1. ✅ Создана микросервисная архитектура
2. ✅ Настроены DTO и API контроллеры
3. ✅ Создан Telegram бот клиент
4. ✅ Добавлены Docker файлы
5. ✅ Созданы скрипты запуска
6. 🔄 Подключить базу данных к API
7. 🔄 Перенести логику из основного проекта в API
8. 🔄 Обновить сайт для работы с API
9. 🔄 Доработать VK бот
10. 🔄 Добавить аутентификацию в API